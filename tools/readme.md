# Tools

Reserved for operational scripts and helper utilities. Run them from the
repository root so imports and relative paths resolve consistently.

This directory is intentionally empty in the template.

The service itself is started with:

```bash
python app/main.py --config /path/to/config.yaml
```
